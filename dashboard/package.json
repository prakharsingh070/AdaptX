{
  "name": "adaptx-dashboard",
  "private": true,
  "version": "0.1.0",
  "description": "ADAPT-X engineering console: static ES modules served by the backend. No dependencies, no build step.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
